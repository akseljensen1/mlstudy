% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_compare.R
\name{ml_compare}
\alias{ml_compare}
\title{Compare three model families on a binary classification task}
\usage{
ml_compare(
  data,
  target,
  prop_train = 0.8,
  seed_split = 3,
  seed_tune = 42,
  cv_folds = 5,
  rf_trees = 300,
  xgb_trees = 300,
  grid_levels = 4
)
}
\arguments{
\item{data}{A data frame containing all predictors and the response.}

\item{target}{Character string: name of the binary factor response column.}

\item{prop_train}{Fraction of rows for training (passed to \code{\link[=ml_split]{ml_split()}}).
Default \code{0.8}.}

\item{seed_split}{Random seed for the train/test split. Default \code{3}.}

\item{seed_tune}{Random seed for cross-validation and tuning. Default \code{42}.}

\item{cv_folds}{Number of cross-validation folds. Default \code{5}.}

\item{rf_trees}{Number of trees for the Random Forest. Default \code{300}.}

\item{xgb_trees}{Number of trees for XGBoost. Default \code{300}.}

\item{grid_levels}{Number of values per hyper-parameter in the tuning grid
(passed to \code{\link[dials:grid_regular]{dials::grid_regular()}}). Default \code{4}.}
}
\value{
An object of class \code{mlstudy_result} (see \code{\link[=new_mlstudy_result]{new_mlstudy_result()}}).
}
\description{
Runs the full tidymodels pipeline mirroring the self-study 1 analysis:
}
\details{
\enumerate{
\item \strong{Split} - stratified 80/20 train/test partition via \code{\link[=ml_split]{ml_split()}}.
\item \strong{Recipe} - dummy-encodes nominal predictors, removes zero-variance
columns, and normalises numeric predictors.
\item \strong{Models} - specifies three families with tunable hyper-parameters:
Random Forest (\link[ranger:ranger]{ranger}), Elastic Net logistic regression
(\link[glmnet:glmnet]{glmnet}), and XGBoost (\link[xgboost:xgboost]{xgboost}).
\item \strong{Grid search} - runs \code{\link[tune:tune_grid]{tune::tune_grid()}} on \code{cv_folds}-fold
cross-validation for each model family using \code{\link[dials:grid_regular]{dials::grid_regular()}}.
\item \strong{Selection} - picks the model with the highest mean CV ROC AUC.
\item \strong{Final fit} - finalises the winning workflow with its best
hyper-parameters and evaluates it on the held-out test set via
\code{\link[tune:last_fit]{tune::last_fit()}}.
}
}
\examples{
\donttest{
library(modeldata)
data(credit_data)
credit_data <- tidyr::drop_na(credit_data)
result <- ml_compare(credit_data, target = "Status")
print(result)
plot(result)
}
}
