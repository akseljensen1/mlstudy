% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlstudy_result.R
\name{new_mlstudy_result}
\alias{new_mlstudy_result}
\title{Create an mlstudy_result object}
\usage{
new_mlstudy_result(
  split,
  target,
  cv_compare,
  best,
  tune_results,
  best_params,
  final_wf,
  final_res,
  test_preds,
  test_metrics
)
}
\arguments{
\item{split}{An \code{rsplit} object from \code{\link[rsample:initial_split]{rsample::initial_split()}}.}

\item{target}{Character string: name of the binary factor response column.}

\item{cv_compare}{A data frame (one row per model) with at least columns
\code{model}, \code{mean} (CV ROC AUC), and \code{std_err}.}

\item{best}{Character string: name of the winning model.}

\item{tune_results}{Named list of \code{tune_results} objects - one per model family.}

\item{best_params}{Named list of data frames - best hyper-parameters per model.}

\item{final_wf}{The finalised \code{workflow} object for the best model.}

\item{final_res}{The \code{last_fit} result (from \code{\link[tune:last_fit]{tune::last_fit()}}).}

\item{test_preds}{Data frame of test-set predictions from
\code{\link[tune:collect_predictions]{tune::collect_predictions()}}.}

\item{test_metrics}{Data frame of test-set metrics from
\code{\link[tune:collect_predictions]{tune::collect_metrics()}}.}
}
\value{
An object of class \code{mlstudy_result}.
}
\description{
Constructor for the S3 class \code{mlstudy_result}. This object stores the
results of a full tidymodels classification pipeline: the train/test split,
cross-validated tuning results for three model families, the selected best
model, the finalised workflow, and test-set predictions and metrics.
}
\examples{
# Normally produced by ml_compare(); direct construction is for testing only.
obj <- new_mlstudy_result(
  split        = NULL,
  target       = "Status",
  cv_compare   = data.frame(model = "RF", mean = 0.84, std_err = 0.01),
  best         = "RF",
  tune_results = list(),
  best_params  = list(),
  final_wf     = NULL,
  final_res    = NULL,
  test_preds   = data.frame(),
  test_metrics = data.frame()
)
class(obj)
}
