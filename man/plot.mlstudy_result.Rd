% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlstudy_result.R
\name{plot.mlstudy_result}
\alias{plot.mlstudy_result}
\title{Plot an mlstudy_result}
\usage{
\method{plot}{mlstudy_result}(x, ...)
}
\arguments{
\item{x}{An object of class \code{mlstudy_result}.}

\item{...}{Further arguments (currently unused).}
}
\value{
Invisibly returns \code{x}.
}
\description{
Produces two ggplot2 panels: (1) a bar chart of cross-validated ROC AUC
for each model family (best model highlighted in green), and (2) the ROC
curve for the best model evaluated on the held-out test set.
}
\examples{
\donttest{
library(modeldata)
data(credit_data)
credit_data <- tidyr::drop_na(credit_data)
result <- ml_compare(credit_data, target = "Status")
plot(result)
}
}
