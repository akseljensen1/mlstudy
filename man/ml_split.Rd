% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_split.R
\name{ml_split}
\alias{ml_split}
\title{Split a data frame into stratified training and test sets}
\usage{
ml_split(data, target, prop_train = 0.8, seed = 3)
}
\arguments{
\item{data}{A data frame containing all predictors and the response.}

\item{target}{Character string: name of the binary factor response column.
Used as the stratification variable.}

\item{prop_train}{Numeric in (0, 1): fraction of rows assigned to training.
Default is \code{0.8} (80 / 20 split).}

\item{seed}{Integer random seed for reproducibility. Default \code{3}.}
}
\value{
An \code{rsplit} object. Use \code{\link[rsample:initial_split]{rsample::training()}} and
\code{\link[rsample:initial_split]{rsample::testing()}} to extract the two sets, or pass the object
directly to \code{\link[=ml_compare]{ml_compare()}}.
}
\description{
A thin wrapper around \code{\link[rsample:initial_split]{rsample::initial_split()}} that enforces stratified
sampling so that both the training and test sets preserve the class
distribution of the response variable.
}
\examples{
library(modeldata)
data(credit_data)
credit_data <- tidyr::drop_na(credit_data)
split <- ml_split(credit_data, target = "Status")
nrow(rsample::training(split))
nrow(rsample::testing(split))
}
